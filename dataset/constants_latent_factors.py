RHYTHMIC_ENTROPY_LIST = range(10)

RHYTHMIC_ENTROPY_DICT = {
    i: t for i, t in enumerate(RHYTHMIC_ENTROPY_LIST)
}

UNIQUE_PITCHES_LIST = range(2, 12)

UNIQUE_PITCHES_DICT = {
    i: t for i, t in enumerate(UNIQUE_PITCHES_LIST)
}

INTERVAL_LIST = range(5, 15)

INTERVAL_DICT = {
    i: t for i, t in enumerate(INTERVAL_LIST)
}
